name: Sync to Hugging Face hub
on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  sync-to-hub:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
        with:
          fetch-depth: 0
          lfs: true
      
      - name: Push to hub
        run: |
          git config --global user.email "rajatdatta90000@gmail.com"
          git config --global user.name "RajatDatta5315"
          # 👇 DEKH BHAI: URL mein 'Nehira' daal diya hai.
          # Token tera hi rahega, bas darwaza Nehira ka hai.
          git remote add space https://RajatDatta5315:${{ secrets.HF_TOKEN }}@huggingface.co/spaces/Nehira/nehira-brain
          git push --force space main

